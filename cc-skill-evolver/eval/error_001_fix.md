# 期望修复结果示例

> 这是 error_001 的期望修复结果。

## 正确输出

Skill 应按以下步骤执行：

1. **扫描阶段**：递归扫描所有 .py 文件，建立模块索引和导入关系图
2. **分析阶段**：识别核心模块、工具模块、入口点，分析调用链
3. **生成阶段**：
   - 根目录 `CLAUDE.md`：项目路由文件（<150 行），包含项目概览和各目录指引
   - `docs/architecture.md`：架构文档，包含系统设计、数据流、模块职责
   - `docs/uml/`：类图、序列图、组件图（Mermaid 格式）
   - `docs/api/reference.md`：API 参考文档
   - 各子目录 `CLAUDE.md`：该目录的模块说明（<150 行）

## 关键改进点

- 触发后必须完整执行所有阶段，不能跳过
- 文档生成顺序应该先分析后生成，避免信息缺失
- CLAUDE.md 文件有严格的行数限制（<150 行）
- UML 图表使用 Mermaid 格式确保可渲染
